/* Aqui como no mesmo JSON do backend temos um array (vetor) que tem vários objetos dentro
 nós vamos usar esse arquivo para representar esse array e os objetos dentro dele */

export type  CategoryDTO = {
    /* tipos dos objetos dentro do array */
    id: number;
    name: string;

};